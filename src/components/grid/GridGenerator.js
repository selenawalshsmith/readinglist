import {chunk} from 'lodash';
import * as React from 'react';
import {Col, Row, Grid} from 'recat-flexbox-grid';
/*
type numOfColumns = {
  cols: 4;
}
const GridGenerator(numOfColumns, children) => {
  const colWidth = 12/numOfColumns;

  const rows = chunk(this.state.myBooks.)
}
*/
